stages:
  - build
  - test
  - deploy
 
Build:
  image: node:17.3.0
  stage: build
  script:
    - cd Backend
    - npm install
    - echo "BUILD SUCCESSFUL APP"
  artifacts:
    paths:
      - Backend/
 
Test:
  image: node:17.3.0
  stage: test
  script:
    - echo "All test Passed"
 
deploy:
  stage: deploy
  image: python:latest
  before_script:
    - apt-get update && apt-get install
    - pip install --upgrade pip
    - pip install awscli
    - aws configure set aws_access_key_id AKIAVIRXQ624H7MAUN6T
    - aws configure set aws_secret_access_key 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
    - aws configure set region us-east-1
  script:
    - $G build -x test -x distTar # # Build the project with Gradle
    - $G distZip  # creates distribution zip for deployment
    - aws s3 cp $BUNDLE_SRC s3://glass-shop-codepipeline-deployment/ --recursive --endpoint-url https://glass-shop-codepipeline-deployment.s3.amazonaws.com/