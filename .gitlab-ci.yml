stages:
  - build
  - test
  - deploy
 
Build:
  image: node:17.3.0
  stage: build
  script:
    - cd Backend
    - npm install
    - echo "BUILD SUCCESSFUL APP"
  artifacts:
    paths:
      - Backend/
 
Test:
  image: node:17.3.0
  stage: test
  script:
    - echo "All test Passed"
 
deploy:
  stage: deploy
  image: python:latest
  script:
    - pip install --upgrade pip
    - pip install awscli
    - cd Backend
    - aws s3 cp . s3://glass-shop-codepipeline-deployment/ --recursive